<form method="post" action="{{ path('career_step_delete', {'id': career_step.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ career_step.id) }}">
    <button class="btn">Delete</button>
</form>
